{
  "private": true,
  "version": "0.0.0",
  "files": [
    "code/",
    "esm/"
  ],
  "type": "module",
  "exports": {
    "./esm/*": "./esm/*",
    ".": "./esm/index.js"
  },
  "workspaces": [],
  "dependencies": {},
  "devDependencies": {},
  "scripts": {
    "clean": "rm -rf ./esm/*",
    "prebuild": "test -d ./node_modules || npm ci",
    "build": "rollup --config",
    "watch": "rollup --config --watch",
    "prestart": "npm -s run build",
    "start": "node --watch --enable-source-maps ./esm/index.js",
    "pretest": "test -f ./esm/index.js || npm -s run build",
    "test": "true"
  }
}
