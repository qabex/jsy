{
  "private": true,
  "version": "0.0.0",
  "files": [
    "code/",
    "esm/"
  ],
  "type": "module",
  "exports": {
    "./esm/*": "./esm/*",
    ".": "./esm/index.mjs"
  },
  "dependencies": {},
  "devDependencies": {},
  "scripts": {
    "clean": "rm -rf ./esm/*",
    "build": "rollup --config",
    "watch": "npm -s run build -- --watch",
    "pretest": "npm -s run build",
    "test": "true"
  }
}
